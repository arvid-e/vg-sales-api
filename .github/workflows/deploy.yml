name: Auto Deploy to Private Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code from GitHub
        run: |
          cd ~/vg-sales-api
          git pull origin main

      - name: Install dependencies
        run: |
          cd ~/vg-sales-api
          # npm install

      - name: Restart API
        run: |
          # Use process manager to refresh the app
          # PM2:
          # pm2 restart vg-sales-api